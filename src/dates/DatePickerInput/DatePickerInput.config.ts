import { ExtendsRootComponent } from "@mantine/core/lib/core/factory/factory"
import { DatePickerInputFactory } from "@mantine/dates"

export const DatePickerInputConfig: ExtendsRootComponent<DatePickerInputFactory> = {
    defaultProps: {
        size: "md",
    },
}
