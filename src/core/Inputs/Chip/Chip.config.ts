import { ExtendsRootComponent } from "@mantine/core/lib/core/factory/factory"
import { ChipFactory } from "@mantine/core"

export const ChipConfig: ExtendsRootComponent<ChipFactory> = {
    defaultProps: {
        color: "mexican-pink",
        radius: "sm",
    },

}
