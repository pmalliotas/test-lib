import { ExtendsRootComponent } from "@mantine/core/lib/core/factory/factory"
import { FileInputFactory } from "@mantine/core"

export const FileInputConfig: ExtendsRootComponent<FileInputFactory> = {
  defaultProps: {
    size: "md"
  },
}
