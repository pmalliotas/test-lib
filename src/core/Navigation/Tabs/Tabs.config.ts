import { ExtendsRootComponent } from "@mantine/core/lib/core/factory/factory"
import { TabsFactory } from "@mantine/core"

export const TabsConfig: ExtendsRootComponent<TabsFactory> = {
    defaultProps: {

    },
}
